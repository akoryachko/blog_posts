Run `make init` to set up the virtual environment with all the dependencies.

Select the notebook kernel `pyspark_to_production_blogpost` in VSCode after that.
Restart VSCode if the Kernel is not visible.

